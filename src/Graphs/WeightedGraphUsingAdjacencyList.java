package Graphs;



public class WeightedGraphUsingAdjacencyList {
/* 
    public static void main(String[] args) {
        int n = 5;
        List<List<Pair<Integer, Integer>>> adj = new ArrayList<>();

        // Adding n+1 new ArrayList to adj
        for (int i = 0; i <= n; i++) {
            adj.add(new ArrayList<>());
        }

        // For Edge between 1 and 2 with weight 3
        adj.get(1).add(new Pair<>(2, 3));
        adj.get(2).add(new Pair<>(1, 3));

        // For Edge between 1 and 3 with weight 5
        adj.get(1).add(new Pair<>(3, 5));
        adj.get(3).add(new Pair<>(1, 5));

        // For Edge between 3 and 4 with weight 2
        adj.get(3).add(new Pair<>(4, 2));
        adj.get(4).add(new Pair<>(3, 2));

        // For Edge between 2 and 4 with weight 7
        adj.get(2).add(new Pair<>(4, 7));
        adj.get(4).add(new Pair<>(2, 7));

        // For Edge between 2 and 5 with weight 4
        adj.get(2).add(new Pair<>(5, 4));
        adj.get(5).add(new Pair<>(2, 4));

        // For Edge between 4 and 5 with weight 6
        adj.get(4).add(new Pair<>(5, 6));
        adj.get(5).add(new Pair<>(4, 6));

        // Print the adjacency list with weights
        for (int i = 1; i <= n; i++) {
            System.out.print("Vertex " + i + " is connected to: ");
            for (Pair<Integer, Integer> neighbor : adj.get(i)) {
                System.out.print("(" + neighbor.getKey() + ", " + neighbor.getValue() + ") ");
            }
            System.out.println();
        }
    }
*/
}